/**
 * Signal Design System — Breakpoint tokens
 *
 * Canonical breakpoint values. Consumed by src/theme/breakpoints.js.
 * Custom keys `mlgg` and `mlg` are preserved (not standard MUI).
 */

export const values = {
  xs: 0,
  sm: 600,
  md: 960,
  lg: 1280,
  mlgg: 1325,
  mlg: 1439,
  xl: 1920,
};

const breakpoints = {
  values,
};

export default breakpoints;
